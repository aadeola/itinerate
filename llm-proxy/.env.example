# Cursor API key from https://cursor.com/dashboard/integrations
CURSOR_API_KEY=crsr_your_key_here
PORT=8081

# Google Maps Platform API key (Places + Geocoding enabled) for the
# /v1/maps/resolve endpoint, which drives the Google Maps MCP.
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
